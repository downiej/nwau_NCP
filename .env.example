# Local dev only — copy to .env (or export to your shell) before running.
# Production deployment uses Azure Functions app settings instead.

NEP_PRICE=7258
NEP_YEAR=2025-26
NEP_API_KEY=
# NEP_XLSX_PATH=C:/path/to/alternative/price_weights.xlsx
